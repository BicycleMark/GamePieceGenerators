/**
 * Simple tests to verify Jest setup
 */

describe('Simple tests', () => {
  test('true is true', () => {
    expect(true).toBe(true);
  });
  
  test('1 + 1 = 2', () => {
    expect(1 + 1).toBe(2);
  });
});
